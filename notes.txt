<script>
        function setRelay(channel, state) {
            // Existing setRelay function code
        }
    
        // Fetch sensor data and update display elements
        function updateSensorData() {
            // Existing updateSensorData function code
        }
    
        // Update gauge value when sensor data is fetched
        function updateGaugeValue(value, type) {
            // Existing updateGaugeValue function code
        }
    
        // Create the gauges instances
        document.addEventListener("DOMContentLoaded", function() {
            temperatureGauge = Gauge(
                document.getElementById("temperature-gauge"),
                // Gauge options
            );
    
            humidityGauge = Gauge(
                document.getElementById("humidity-gauge"),
                // Gauge options
            );
    
            moistureGauge = Gauge(
                document.getElementById("moisture-gauge"),
                // Gauge options
            );
    
            // New gauge instance
            gauge3 = Gauge(
                document.getElementById("gauge3"),
                {
                    max: 100,
                    value: 50
                }
            );
        });
        setInterval(updateSensorData, 5000);
    </script>